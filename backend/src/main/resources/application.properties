# =============================================
# Smart Campus — Application Properties
# =============================================

spring.application.name=smart-campus

# ── Supabase PostgreSQL ──
spring.datasource.url=jdbc:postgresql://db.ufcjihmtsidoyikaszic.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=${SUPABASE_DB_PASSWORD:YOUR_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ── File Upload ──
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=15MB

# ── Supabase Storage ──
supabase.url=https://ufcjihmtsidoyikaszic.supabase.co
supabase.key=sb_publishable_CYnWjsmBW7cirFaoGLn_1Q_BJZph79g

# ── Server ──
server.port=8080
